#!/bin/bash

compiler=cc

files=()

while [ -n "$1" ]; do
	case "$1" in
		--)
			shift
			$compiler "$@" "${files[@]}"
			break
			;;
		-*)
			echo found argument "$1"
			;;
		*) # Assume arg is file. Pass to compiler
			files+=("$1")
			;;
	esac
	shift
done
